---
import PlayListItemCard from '../components/PlayListItemCard.astro';
import Greeting from '../components/Greeting.svelte';
import Layout from '../layouts/Layout.astro';
import { playlists, songs } from '../lib/data';
import SongsItemCard from '../components/SongsItemCard.astro';
---

<Layout title="Spotify Clone">
  <div
    id="playlist-container"
    class="relative transition-all duration-1000 bg-red-900"
  >
    <div class="relative z-10 px-6 pt-10">
      <Greeting />
      <div class="flex flex-wrap mt-6 gap-4">
        {playlists.map((playlist) => <PlayListItemCard playlist={playlist} />)}
      </div>
      <div
        class="absolute inset-0 bg-linear-to-t from-zinc-900 via-zinc-900/80 -z-1"
      >
      </div>
      <div class="flex flex-wrap mt-6 gap-4">
        <h1 class="w-full text-5xl mt-10">Canciones del Momento</h1>
        {songs.map((song) => <SongsItemCard song={song} />)}
      </div>
    </div>
  </div>
</Layout>
